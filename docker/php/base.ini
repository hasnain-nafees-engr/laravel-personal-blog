; Settings shared by every environment.
memory_limit = 256M

; Cover image uploads: validated at 2 MB per file in the app, but leave
; headroom for multi-part form overhead and future multi-file forms.
upload_max_filesize = 10M
post_max_size = 12M

expose_php = Off
date.timezone = UTC
